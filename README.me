to play game:
1. go to the directory for this project in your terminal
	to do this, open Terminal.app, then type "cd ", then open finder and drag the folder into the terminal window.  The path to that
	folder should appear in the terminal (something like "cd /Users/afox/GameTest"). Press enter and you will go to the folder.
2. type "chmod +x install-frameworks.sh"
3. type "./install-frameworks.sh"
4. type "./Game"
5. play game
